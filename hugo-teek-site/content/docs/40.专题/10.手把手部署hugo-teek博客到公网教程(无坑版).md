---
categories:
    - 博客
coverImg: https://img2.onedayxyy.cn/images/image-20260120205855.avif
date: "2025-11-18T02:18:43.83471+08:00"
description: "3s部署，早日享受\U0001F60A"
draft: false
featured: true
tags:
    - hugo-teek
title: 手把手部署hugo-teek博客到公网教程(无坑版)
url: /topic/hugo-teek-install
weight: 2
---
## 手把手部署hugo-teek博客到公网教程(无坑版)

本次是`v2-2026.2.7版本`。

> 使用`eo+cos`来部署自己的hugo-teek博客到公网。（0服务器方案，0开销方案，快如闪电的部署方式）😜

![](https://img2.onedayxyy.cn/images/image-20260120205855.avif)

## 最终效果

One博客 https://onedayxyy.cn/ （本人最终效果如下，大家可先尽情体验下。😜）

![](https://img2.onedayxyy.cn/images/image-20260122203935.avif)

## 写在前面

::: warning 告警

Hugo-teek 目前正处于余温作者开发`配置中心`阶段，可能会频繁地进行更新。且`一键web更新`还未开发完成，因此，强烈建议新进群的小伙伴不要去修改博客一些自定义的信息，你需要做的就是，把hugo-teek博客部署起来，然后把自己的md文档放到`hugo-teek-site\content\docs\40.专题`路径。然后发布到自己博客就好。

等后续一键web更新完全开发完成后，我们就可以在配置中心配置下自己的用户信息了，后续有新版出现时，我们一键web更新就可以升级到最新版。

目前的hugo-teek做个人博客/文档库完全是没问题的，但是想更加丝滑体验这款强大的博客，大家再耐心等待下，等我们伟大的余温群主王者归来。😜

:::

## 部署环境

1. 有一台windows电脑（本方案是，win本地维护md文档，然后博客托管在腾讯云eo边缘函数上。）
2. 电脑上安装好了git
3. 注册[CNb](https://cnb.cool/)账号（存储hugo-teek仓库代码及md核心文档）
4. 注册[腾讯eo](https://cloud.tencent.com/login?s_url=https%3A%2F%2Fconsole.cloud.tencent.com%2Fedgeone%2Fzones)账号（用来新建cos存储桶）
5. 拥有自己的域名
6. 有手就好😜



::: warning 注意：🚩

（1）本次部署的环境是，我们在windows本地把hugo-teek仓库拉下来，然后把自己的md文档目录放到仓库对应`hugo-teek-site\content\docs\40.专题`路径，然后推送到cnb仓库，同时会触发cnb自动构建，进而发布网站。

（2）我们的hugo-teek静态dist产物是通过cnb流水线，云端构建完成后被推送到cos，再配合eo，用户就能正常访问咋们的博客了。我们只需要重点维护好自己仓库里的md文档就行。

:::

## 快速体验⚡️

1️⃣方案1：常规部署方式 是将hugo-teek博客源码拉取到win本地，再通过cnb流水线推送到cos，进而发布自己博客；（本文重点讲解的方式。）

2️⃣方案2：也有一种更快速的方案，就是直接fork群主[hugo-teek](https://cnb.cool/yuwen-gueen/hugo-teeker-theme)项目到自己cnb，然后点击`Hugo-Teek宇宙启动🚀`，就会自动打开cnb云开发控制台，也同时会自动打开博客运行界面，通过执行`sh bin/linux/start-admin.sh `脚本，也会立马运行hugo-teek的在线后台，非常方便。(前提是得先拥有自己的[cnb](https://cnb.cool/)账号即可。)

> 这个是在cnb云开发linux环境上运行的，让然win环境也是可以这样操作的，只是目前作者先优先win开发的，等功能彻底开发完成后，win上的功能也会修复，耐心等待吧。😉

![](https://img2.onedayxyy.cn/images/image-20260207075944.avif)

<video src="https://img2.onedayxyy.cn/images/20260207_075502.mp4" controls=""></video>



## 前置条件

### 1.购买cos资源包

* 来到自己[腾讯云对象存储(cos)](https://buy.cloud.tencent.com/cos?packageType=std)下，我们来购买一个资源包

![](https://img2.onedayxyy.cn/images/image-20260125183232.avif)

### 2.创建存储桶

来到[腾讯创建cos存储桶](https://console.cloud.tencent.com/cos/bucket)地址，我们创建下存储桶：

![](https://img2.onedayxyy.cn/images/image-20260125202742.avif)

![](https://img2.onedayxyy.cn/images/image-20260125183550.avif)

> - 所属地域 选择`广州`；
> - 访问权限选择`私有读写`；
> - 记得要关闭`多AZ特性`；
> - 其余点击下一步就好。

![](https://img2.onedayxyy.cn/images/image-20260125183715.avif)

![](https://img2.onedayxyy.cn/images/image-20260125183754.avif)

### 3.获取腾讯云密钥

来到[获取腾讯云密钥](https://console.cloud.tencent.com/cam/capi)网址：

新建自己的密钥，记得保存好，后续要用：

![](https://img2.onedayxyy.cn/images/image-20260125203356.avif)

![](https://img2.onedayxyy.cn/images/image-20260125203503.avif)

## 1、拉取代码

### 1.拉取项目

我们来到自己的d盘，然后拉取项目：

```bash
cd /d/
git clone https://cnb.cool/yuwen-gueen/hugo-teeker-theme.git
cd hugo-teeker-theme
```



🟢 双击运行

进入到`bin\windows`目录，双击运行`start.bat`脚本：

![](https://img2.onedayxyy.cn/images/image-20260112101545.avif)

![image-20251220022314233](https://img.onedayxyy.cn/images/image-20251220022314233.avif)



🟢  访问测试

在浏览器输入http://localhost:8080/ 验证效果：

![image-20251118021944048](https://img.onedayxyy.cn/images/image-20251118021944048.avif)



🟢 我们再将本地项目推送到自己的cnb仓库

📢📢📢
::: details 具体步骤：

（1）创建私有库（存放hugo-teek源码及md核心文档）

* 来到自己的[CNb](https://cnb.cool/)主页，创建一个私有仓库，名称叫做`hugo-teek-private`；

* 把上面拉取的代码，直接推送到这个私有库：

  再推送之前，先把我们本地的项目名称重命名为`hugo-teek-private`,然后推送；

  ```bash
  cd /d/hugo-teek-private
  rm -rf .git ##记得先删除之前的.git
  
  git init .
  git remote add origin https://cnb.cool/onedayxyy/hugo-teek-private
  git add -A
  git commit -m"first commit"
  git push --set-upstream origin master
  # git push
  ```

  


（2）我们还需要编辑下项目下`.cnb.yml`文件内容：（这里将用户cnb一键流水线发布博客使用。）

> 以下文件我们只需要更改如下一处位置即可：（其余无需配置）

```bash
      imports:
        - https://cnb.cool/onedayxyy/secret/-/blob/main/envs.yml    #这里存放自己需要用到的密钥,将这里修改为自己密钥路径。  
```

`.cnb.yml`文件内容：

```yaml
master:
  push:
    - runner:
        cpus: 64

      imports:
        - https://cnb.cool/onedayxyy/secret/-/blob/main/envs.yml        

      docker:
        image: docker.cnb.cool/yuwen-gueen/docker-images-chrom/hugo-teek-blog:latest

      stages:
        # ------------------------------
        # 1️⃣ 记录开始时间
        # ------------------------------
        - name: set env
          script: echo -n $(date +%s)
          exports:
            info: CUSTOM_ENV_START_TIME

        # ------------------------------
        # 4️⃣ 构建 Hugo
        # ------------------------------
        - name: build
          script: |
            make build-docker

        - name: 部署到 cos
          image: tencentcom/coscli        
          script: |
            coscli config set --secret_id $COS_SECRET_ID --secret_key $COS_SECRET_KEY
            coscli config add --init-skip=true -b $COS_BUCKET -r $COS_REGION
            coscli sync ./hugo-teek-site/public cos://$COS_BUCKET/ -r \
              --delete --force --routines=1000 --thread-num=1000


        - name: 🧘‍♂️ 刷新缓存
          image: docker.cnb.cool/znb/cdn-refresh
          settings:
            ak: "${COS_SECRET_ID}"
            sk: "${COS_SECRET_KEY}"
            kind: "tencenteo"
            rtype: "path"
            domain: "onedayxyy.cn"
            urls:
              - "https://onedayxyy.cn/"

        # ------------------------------
        # 6️⃣ 计算耗时
        # ------------------------------
        - name: ⏱️ 计算耗时
          script: |
            end_time=$(date +%s)
            duration=$((end_time - $CUSTOM_ENV_START_TIME))
            minutes=$((duration / 60))
            seconds=$((duration % 60))
            echo -n "${minutes}分${seconds}秒"
          exports:
            info: CUSTOM_ENV_BUILD_TIME

        # ------------------------------
        # 7️⃣ 钉钉通知
        # ------------------------------
        - name: 钉钉通知
          image: docker.cnb.cool/yuwen-gueen/docker-images-chrom/tencentcom-dingtalk-bot-msg:latest_amd64
          settings:
            content: "「Hugo-Teek One Blog」发布完成，耗时: ${CUSTOM_ENV_BUILD_TIME}"
            c_type: "text"
            secret: $SECRET
            webhook: $WEBHOOK
            isAtAll: false
            debug: false

include:
  - .cnb/vscode.yml
```



注意：

![](https://img2.onedayxyy.cn/images/image-20260126192507.avif)



自己`cnb密钥仓库`内容如下：（如果没有这个仓库，需要先新建一个自己的密钥仓库）这里要填的内容就是前面我们获取的腾讯云密钥信息，填好就行。

`https://cnb.cool/onedayxyy/secret/-/blob/main/envs.yml`

```bash
# cos.yml
COS_SECRET_ID: 你自己的COS_SECRET_ID
COS_SECRET_KEY: 你自己的COS_SECRET_KEY
COS_BUCKET: 你自己的COS_BUCKET
COS_REGION: 你自己的COS_REGION
```

![](https://img2.onedayxyy.cn/images/image-20260125202143.avif)

![](https://img2.onedayxyy.cn/images/image-20260125202357.avif)



![](https://img2.onedayxyy.cn/images/image-20260125202537.avif)

:::





`.cnb.yml`文件内容：

```yaml
master:
  push:
    - runner:
        cpus: 64

      imports:
        - https://cnb.cool/onedayxyy/secret/-/blob/main/envs.yml        

      docker:
        image: docker.cnb.cool/yuwen-gueen/docker-images-chrom/hugo-teek-blog:latest

      stages:
        # ------------------------------
        # 1️⃣ 记录开始时间
        # ------------------------------
        - name: set env
          script: echo -n $(date +%s)
          exports:
            info: CUSTOM_ENV_START_TIME

        # ------------------------------
        # 4️⃣ 构建 Hugo
        # ------------------------------
        - name: build
          script: |
            make build-docker

        - name: 部署到 cos
          image: tencentcom/coscli        
          script: |
            coscli config set --secret_id $COS_SECRET_ID --secret_key $COS_SECRET_KEY
            coscli config add --init-skip=true -b $COS_BUCKET -r $COS_REGION
            coscli sync ./hugo-teek-site/public cos://$COS_BUCKET/ -r \
              --delete --force --routines=1000 --thread-num=1000


        - name: 🧘‍♂️ 刷新缓存
          image: docker.cnb.cool/znb/cdn-refresh
          settings:
            ak: "${COS_SECRET_ID}"
            sk: "${COS_SECRET_KEY}"
            kind: "tencenteo"
            rtype: "path"
            domain: "onedayxyy.cn"
            urls:
              - "https://onedayxyy.cn/"

        # ------------------------------
        # 6️⃣ 计算耗时
        # ------------------------------
        - name: ⏱️ 计算耗时
          script: |
            end_time=$(date +%s)
            duration=$((end_time - $CUSTOM_ENV_START_TIME))
            minutes=$((duration / 60))
            seconds=$((duration % 60))
            echo -n "${minutes}分${seconds}秒"
          exports:
            info: CUSTOM_ENV_BUILD_TIME

        # ------------------------------
        # 7️⃣ 钉钉通知
        # ------------------------------
        - name: 钉钉通知
          image: docker.cnb.cool/yuwen-gueen/docker-images-chrom/tencentcom-dingtalk-bot-msg:latest_amd64
          settings:
            content: "「Hugo-Teek One Blog」发布完成，耗时: ${CUSTOM_ENV_BUILD_TIME}"
            c_type: "text"
            secret: $SECRET
            webhook: $WEBHOOK
            isAtAll: false
            debug: false

include:
  - .cnb/vscode.yml
```



### 2.更新文档

以上，我们的hugo-teek博客已经下载并部署好了，那么该如何新增自己的md文档呢?

把自己的md文档放到`hugo-teek-site\content\docs\40.专题`路径下就好（无论是目录或者md文档都行），就这么简单。

我的截图如下：

![](https://img2.onedayxyy.cn/images/image-20260112154113.avif)

### 3.配置信息

> 待余温作者把hugo-teek的配置中心配置完成后，我们就可以本地运行图形化来配置博客选项了，非常简单。我们耐心等待吧。😉



当然，如果你想现在就自定义自己的博客配置，那么可以看我如下文档：

《自定义配置信息》https://onedayxyy.cn/topic/grhke （跟着文档配置，很简单，立马就可以配置为自己博客的一些信息。😜）

![](https://img2.onedayxyy.cn/images/image-20260207075000.avif)



以上，我们的hugo-teek博客是可以在本地快乐地运行了，但是如何把这个博客部署到公网，别人也可以访问呢？请看下文。

> 使用`eo+cos存储桶`来部署自己的hugo-teek博客到公网。（0服务器方案）😜

## 2、发布博客

### 1.配置域名

这里做好[eo的域名配置](https://cloud.tencent.com/login?s_url=https%3A%2F%2Fconsole.cloud.tencent.com%2Fedgeone%2Fzones)：

![](https://img2.onedayxyy.cn/images/image-20260125184034.avif)

![](https://img2.onedayxyy.cn/images/image-20260125184101.avif)

### 2.开启`静态网站`

来到自己刚创建好的cos存储桶位置：

> 🔴 记得要在这个存储桶下开启`静态网站`功能哦！！！

![](https://img2.onedayxyy.cn/images/image-20260125184206.avif)

### 3.缓存配置

来到[eo的缓存配置](https://console.cloud.tencent.com/edgeone/zones/detail/zone-3dsj112iir36/acceleration/global-setting)，我们开启几个配置：

- 记得配置下`节点缓存TTL`、`浏览器缓存TTL`缓存过期时间为30天；
- 开启下离线缓存；
- 开启HSTS配置（保持默认即可）；
- 开启 强制https 功能（需要设置为重定向方式：301）；

![](https://img2.onedayxyy.cn/images/image-20260125204835.avif)

![](https://img2.onedayxyy.cn/images/image-20260125204943.avif)

![](https://img2.onedayxyy.cn/images/image-20260128103749.avif)

![](https://img2.onedayxyy.cn/images/image-20260128103910.avif)
## 3、访问验证

https://onedayxyy.cn/

![](https://img2.onedayxyy.cn/images/image-20260122203935.avif)

结束。